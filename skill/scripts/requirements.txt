# weight-loss-skill Python 依赖

# 核心
pandas>=2.0.0
openpyxl>=3.1.0

# 可选（用于命令行统计）
tabulate>=0.9.0

# 可选（用于生成 HTML 报告时处理数据）
jinja2>=3.1.0

# 使用方式
#   pip install -r scripts/requirements.txt
#   python scripts/bmr_calculator.py --weight 70 --height 168 --age 21 --male
#   python scripts/analyze_week.py --input week_data.txt --weight 70 --height 168 --age 21 --male
